import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personal.cryptosmart',
  appName: 'Cryptosmart',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
